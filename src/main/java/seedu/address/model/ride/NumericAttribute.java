package seedu.address.model.ride;

/**
 * A numeric attribute is an attribute of the ride that contains a number
 */
public interface NumericAttribute {

    int getValue();

    void setValue(int value);
}
